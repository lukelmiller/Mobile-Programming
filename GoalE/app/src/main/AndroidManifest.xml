<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.csce4623.lukelmiller.goale">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoalE">


        <activity android:name=".editgoalactivity.EditGoalActivity"/>


        <activity android:name=".addgoalactivity.AddGoalActivity" />

        <activity android:name=".categoryProgress.categoryProgressView" />


        <activity android:name=".goalListActivity.FullListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>